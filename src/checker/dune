
(library
  (name quip_check)
  (public_name quip.check)
  (libraries quip.core trustee.core containers ppx_deriving.runtime tracy)
  (preprocess (pps ppx_deriving.std))
  (flags :standard -warn-error -a+8))
