
(library
  (name quip_core)
  (public_name quip.core)
  (synopsis "Core AST and parser for Quip")
  (libraries smtlib-utils trustee.core containers logs
             ppx_deriving.runtime)
  (preprocess (pps ppx_deriving.std))
  (flags :standard -warn-error -a+8))
