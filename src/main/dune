
(executable
  (name quip)
  (public_name quip)
  (libraries quip.core quip.check trustee.core
             smtlib-utils containers ppx_deriving.runtime)
  (preprocess (pps ppx_deriving.std))
  (flags :standard -warn-error -a+8))
